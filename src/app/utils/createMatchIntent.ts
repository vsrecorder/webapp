/*
 * 「この記録の対戦追加モーダルを、遷移先で開いてほしい」という指示を画面間で渡すためのキー。
 *
 * ホームの「記録中」カードの『対戦結果を追加する』は、その場でモーダルを開かず、
 * 記録詳細ページへ移ってから開く。入力を終えたあとに、その記録の対戦一覧と戦績が
 * そのまま目の前にあるほうが、大会の合間に何戦目まで入れたかを確かめやすいため。
 *
 * 値は記録ID。遷移先のボタン(CreateMatchModalButton)が自分の記録と一致するときだけ
 * 消費する(別の記録の詳細ページを開いても誤って開かない)。読んだ時点で消すので、
 * 再読み込みや戻り遷移で開き直すこともない。
 *
 * URL のクエリではなく sessionStorage を使うのは、このアプリが画面間の「開き直し」を
 * すべてそこで受け渡しているため(utils/recordModalReopen と同じ流儀)。
 */
export const OPEN_CREATE_MATCH_RECORD_ID = "openCreateMatchRecordId";
